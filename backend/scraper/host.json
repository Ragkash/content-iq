{
  "_comment": "DEPLOYED TO AZURE: Required config file that tells the Azure Functions runtime which version to use, how to log to Application Insights, and which extension bundle to load for the timer trigger. Azure will not start the Function App without this file.",
  "version": "2.0",
  "logging": {
    "applicationInsights": {
      "samplingSettings": {
        "isEnabled": true,
        "excludedTypes": "Request"
      }
    }
  },
  "extensionBundle": {
    "id": "Microsoft.Azure.Functions.ExtensionBundle",
    "version": "[4.*, 5.0.0)"
  }
}
