# DEPLOYED TO AZURE: This file is read by Azure's Oryx build system during deployment.
# Every package listed here gets pip-installed on Azure's Linux server so the scraper
# can run. Without this file Azure would have no dependencies and the function would crash
# on every import statement.
azure-functions
azure-storage-blob>=12.20.0
azure-search-documents>=11.6.0
openai>=1.30.0
tiktoken>=0.7.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
tenacity>=8.3.0
rich>=13.7.0
