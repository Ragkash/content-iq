# ────────────────────────────────────
# Content IQ — Backend Dependencies
# Install: pip install -r requirements.txt
# ────────────────────────────────────

# LLM (answer generation / query parsing)
groq>=0.9.0

# API Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Azure AI Services
openai>=1.30.0
azure-search-documents>=11.6.0
azure-storage-blob>=12.20.0
azure-identity>=1.16.0

# Tokenisation (for chunking)
tiktoken>=0.7.0

# HTTP client (Content Understanding REST calls, Bing API)
httpx>=0.27.0

# Env / config
python-dotenv>=1.0.0

# Data handling
pydantic>=2.7.0

# Utilities
tenacity>=8.3.0     # retry with exponential backoff
rich>=13.7.0        # pretty CLI logging for ingestion scripts
pypdf>=4.0.0        # PDF text extraction fallback (no CU required)

# Substack scraper
beautifulsoup4>=4.12.0  # extract clean text from Substack HTML

# Eval tooling
anthropic>=0.26.0       # Claude judge for T3 eval scoring (judge_t3.py)
playwright>=1.45.0      # Full-stack browser eval (playwright_eval.py); run: python -m playwright install chromium
